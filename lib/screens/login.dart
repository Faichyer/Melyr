import 'package:flutter/material.dart';
import 'package:melyr/widgets/login/loginBody.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginBody(),
    );
  }
}
